# 批量翻译工具 缺陷记录（防再发清单）

> 规则：每发现一个缺陷，记录现象→根因→修复→验证方式→所在版本。回归时必须逐项复查。

## 2026-08-13（v1.6 修复）

| # | 现象 | 根因 | 修复 | 验证 |
|---|------|------|------|------|
| 1 | 图5俄语横幅出现"□□□"方框字 | GLM 批量翻译偶发混入中文，西里尔字体无中文字形 | 译文质检：非中日韩语种检出中文→判失败重试（批量3次+逐条兜底），仍残留则剔除中文字符 | 回归时质检真实触发并重试成功，横幅纯俄语 |
| 2 | 图1顶部深色污斑矩形 | 白字压深色照片背景时整块抹除，LaMa 填不出照片虚化纹理 | 白字深底（fl>170 且 bl<140）无论背景均匀度都走笔画级抹除；修复区整体柔化 σ=5 模拟焦外虚化 | 污斑消失，背景虚化自然 |
| 3 | 图2标题白色残影 | ①固定阈值(fl±70)漏提取浅色笔画 ②连通域过滤把整行文字误当装饰边框丢弃 | ①笔画提取改 Otsu 自适应阈值（退化时回退固定边距95）②边框判定收紧为 cw≥0.9rw 且 ch≥0.85rh 且填充率<0.30 ③新增二次抹除：修复后重检残影再抹一遍 | 残影强度降至 0.26 灰度级（肉眼不可见） |
| 4 | 图4副标题过小过淡 | 单行小字兜底字号下限过低 | wide_label 最小字号 11→12 | 副标题清晰可读 |
| 5 | 图4青色/橙色标签被整片抹掉（手臂旁缺口） | 新增的二次抹除在已修复图上重检残影时，把纯色标签误判为残留笔画再抹一遍 | 二次抹除跳过检测到彩色标签的区域 | 重出图4四语种，三枚标签完整保留且译文写在标签内 |

## 历史缺陷（v1.0–v1.5 已修）

- 图3黑块：OCR 框漏字 + 渲染溢出 → OCR 框扩展(+35%w/+45%h) + 单行宽度钳制 + 边框锁定渲染
- 图3边框被误抹 → 笔画抹除时大而稀疏连通域判定为装饰边框保留
- 图4彩色标签被抹除 → 标签检测（HSV饱和度）+ 标签内笔画抹除 + 渲染锁定标签内部
- 图5横幅误走笔画抹除 → 背景均匀度门控（uni≥0.5 才允许笔画抹除）
- 抹除矩形接缝 → 掩膜膨胀13+高斯羽化 σ=8 融合
- CI 冒烟误判：画面文字原位替换时不生成 .srt → 冒烟测试改为可选检查
- CI 打包失败：PowerShell 不支持 cmd 的 `^` 续行 → 改反引号

## 2026-08-31 v1.7 新图（收纳七件套）修复
- **LaMa 尺寸对齐回退**：SimpleLama 内部把输入对齐到 8 的倍数（790×1477→792×1480），输出尺寸与 mask 不一致导致羽化融合广播失败，静默回退旧算法把整框（含棕色标签）抹掉 → 两次 LaMa 调用后都裁剪 `out = out[:h, :w]`，且回退路径必须告警日志
- **混色误判极性**：深字+白色光晕/图标混合时 `_estimate_colors` p75 估计落在中间灰甚至 217~233，Otsu 选错极性只罩住几百像素 → 防混色守卫改为 `90 < lum < 250 且背景不更暗` 时用 p95 差异簇重估，浅底（bl>200）下明显更暗即采纳
- **笔画 mask 用膨胀框重估颜色**：`_stroke_mask` 在膨胀 10px 的框内重估前景色，框上图标/邻行文字混入导致极性翻转（fl 52→218），mask 几乎为空 → `_stroke_mask` 增加 `fg` 参数，一律传入未膨胀框的估计色
- **二次抹除不稳定**：二抹在修复结果上重做 Otsu 笔画提取，近均匀浅底上阈值漂移，是否触发整框回退近乎随机（单框干净、14 框联跑残影） → 二抹改为直接与背景色比亮度（深字找暗于 bg-25、白字找亮于 bg+25），并滤掉触及区域外缘的连通块防误伤图标
- **文字旁图标被糊化**：①笔画 mask 把膨胀区内卷入的图标笔画当文字抹掉 → 连通块质心落在未膨胀框外的一律保留；②焦外柔化（高斯模糊 σ5）在浅色均匀背景上把邻近图标糊成雾斑 → 仅在存在非均匀背景框（uni<0.5）时启用柔化
- **小标签译文缩得过小看不清**：彩色小标签强制单行+宽扁规则把字号压到 12px 以下 → 标签模式允许两行（字号至少提升 40%）、高度余量收紧到 1.06 保证不出标签、内缩从 8%/12% 减到 5%/6%
- **葡语斜杠联项误译**：GLM 批量译"鞋子/杂物"为"Sapatos/obrigado"（葡萄牙语"谢谢"）等串译 → 术语库固定三类斜杠联项标签精译
- **同排文字字号不一**：各框独立搜索字号，同排译文长短不同导致字号参差不齐 → 渲染改两遍：先探测各框可行字号，同一水平带（带高相近）分组取组内最小字号强制统一
- **长单词被拦腰截断**：断行器在单词超宽时按字符硬切（"Miscellaneo/us bag"） → 字号搜索跳过放不下最长单词的字号，选定后仍超宽则逐级缩小兜底

## 2026-08-31（双肩包图测试，v1.7 续）

- **跨栏清单误合并**：`merge_line_boxes` 按 (cy,x) 排序后同行相邻框水平间距为负，被当作同一行合并，3×3 清单翻译串栏（"杂志 15.6英寸电脑 钱包"挤成一行）。修复：水平闭合条件改为 `-2 <= gap < min_h*0.6`，负间距不允许合并。
- **低覆盖率颜色估计失效**：加宽框内文字占比 <25% 时 p75 分位取到背景色，fg=bg=255，极性判成白字导致笔画 mask 全空、文字完全没抹。修复：`_estimate_colors` 低覆盖守卫——`max(fg)-min(fg)<=30 且 |fg-bg|max<=30` 时改用距离分布 p95 重估。
- **浅底羽化环残影**：LaMa 填充与浅色底有轻微色差，羽化环把原文字边缘混回结果，留下雾状鬼影。修复：均匀浅底（uni≥0.65 且底色亮度>200）登记平涂清单，混合后在膨胀 25px 核心区内强制贴回底色。
- **整批译错语种**：GLM 偶发把俄语请求答成英语（整批）。修复：`_lang_suspect` 按目标语文字区间校验（西里尔/阿拉伯/泰/印地/希伯来/韩/日等），批量与单条兜底链路命中即抛错重试。
- **标签小数点/数字误抹**：`_label_stroke_mask` 吃掉 15.6 的小数点，且 11px 膨胀晕染到上方数字。修复：跳过质心位于文字行上方的连通域、保护圆点状小组件、膨胀核降为 7、裁剪 inner 顶边以上 15px 的行范围。

## 2026-09-01（表格字号统一，v1.7 续二）

- **表格跨行字号不统一**：同排统一只覆盖单行带，3×3 清单行与行字号各异。修复：行带二次合并——框高相近、列区间重叠≥50%、行距≤4 倍框高的相邻行带视为同一表格统一取最小字号；行带代表框高改用组内最大值（此前用种子框高，遇到 30px 的窄高框直接判定不相似而漏并）；带成员纳入未被首轮分组强制的框（cy 相近、bh 比≥0.45），否则个别框漏统一。
- **限高窄框拖低整表字号**：长译文落在 30px 高的框里只能 9 号单行，整表被迫 9 号。修复：对拉低组内最小字号的成员，按行带最大框高扩框重新探测（允许两行），把整表字号从 9 抬回 18。
- **同表粗细不一**：`_estimate_bold` 按框各自判定，3×3 清单第三行误报粗体。修复：`force_bold` 参数，组内按多数票统一字重。

## 2026-09-01（保温杯手臂区域模糊，v1.7 续三）

- **修复区整体 σ5 柔化糊掉纹理**：复杂背景下柔化作用于整个修复区，毛衣/皮肤交界被糊成雾斑。修复：柔化只作用于遮罩边缘过渡带（m 腐蚀 25px 取边缘带），修复区内部保留填充锐度。
- **LaMa 在结构背景上糊边缘**：笔画级 mask 区域 LaMa 重建会把清晰的材质交界（毛衣/背景）抹成雾。修复：混合修复——笔画级 mask 改用 OpenCV Telea 沿边缘方向扩散填充（保交界锐度），整块 mask（大面积缺失）仍用 LaMa；二次残影抹除同步改 Telea。
- **标签白字走 Telea 留雾状残字**：标签笔画 mask 未盖全的白字边缘被 Telea 等向扩散成雾。修复：彩色标签笔画不进 Telea，统一交 LaMa 语义填充。
- **标签圆点保护误伤中文偏旁**：圆点状组件保护把"温"的氵点、"皿"等偏旁当小数点保留，标签里留下残字。修复：圆点保护收紧——仅当位于顶部数字带（inner 上缘 15px 内）且同排有 ≥2 个明显更组件（数字）时生效。
- **行裁剪保护数字却误伤文字顶**：按 inner 上缘+15px 整行裁剪，OCR 框带 padding 时把单行标签文字笔画顶部裁出 mask 外，留下 ghost tops。修复：改为按组件轮廓精确保护——顶部数字带组件（矮小组件、与下方文字行有明显间隙、下方另有文字行）记入 prot，膨胀后仅从 mask 中挖掉 prot 轮廓。

## 2026-09-01（标签白字残印，v1.7 续四）

- **标签白字半透明描边漏抹**：`_label_stroke_mask` 差异阈值 70，白字描边 d 值集中在 45~70，漏抹留下白色残印。修复：阈值降到 45。
- **LaMa/羽化在纯色标签上留 faint 字影**：即使 mask 覆盖完整，填充色与标签底差几级，字影可见。修复：标签逐行中位色定色——取本行未抹标签像素的中位色填回笔画区（标签为纯色/纵向渐变，逐行采样即真实底色）；填充区严格限定在标签底内部（原图接近标签色的区域膨胀后取交），防止涂出圆角边界。
- **标签边缘阴影组件误入 mask**：贴标签外缘的阴影/描边组件被当文字抹除后又被定色成斑块。修复：触及标签外缘的组件判定为非文字，不抹且记入保护集。
- **标签残印清理误伤小数点**：二抹清理按亮度抓残印，把徽章的小数点当残字抹掉。修复：`_label_stroke_mask` 返回保护集 prot，清理时挖掉 prot 膨胀区。

## 2026-09-01 四图测试（v1.7续五）

- **混色标题亮色笔画整段漏抹**（折叠行李包「一『包』搞定」）：`_stroke_mask` 用 Otsu 按文字主色亮度选极性，主色为黑时亮橙色笔画完全不在 mask 里，橙色「包」原样残留。修复：在 Otsu 结果上按饱和度补抓彩色笔画（S>70 且 V>60；灰/白/黑背景饱和度低不受影响），再统一做连通块过滤。
- **中文框误伤相邻英文框**（折叠行李包「GO TO TRAVEL」被抹成"GO IO IIUI"）：含中文的框膨胀后与下方纯英文框重叠，笔画 mask 把重叠区的英文笔画一起抹了。修复：OCR 阶段把无中文框记入 protect 列表，`erase_text_lama` 新增 protect 参数，抹除 mask 与二次残影 mask 都挖掉保护区。注意第一版"整框清零"会把斜排中文伸进保护框的笔画底部留成残字——改为只保护框内"自身笔画"（与背景差异的连通块中不贴边的内部块，贴边块=邻行中文/纹理侵入不保护）。
- **纹理背景被平涂成死色块**（折叠背包铁丝网背景字幕）：`uni>=0.65 且底色>200` 的平涂定色把"看起来均匀但有纹理"的网格背景整片涂成浅灰，像贴了一块补丁。修复：平涂前加纹理守卫——框内灰度 Sobel 梯度均值 ≥60 判定为纹理背景，跳过平涂（平整白墙/纯色标签梯度 14~37，网格/织物 160+）。

## 2026-09-01 四图测试（v1.7续六）

- **保护区整框清零误留中文残字**：第一版非中文框保护直接整框清零，斜排中文（自由随行）笔画底部伸进英文框区域，底部笔画没被抹掉留下残字。修复：改为只保护框内"自身笔画"——框内与背景差异的连通块中，贴边块（邻行中文/纹理侵入）不保护，内部块膨胀 7px 后保护。
- **俄语译文单条写成英语漏检**（细节展示 RU 版出现"Side pocket display"）：`_lang_suspect` 只查整批是否含目标文字体系，一批里其他条有西里尔就放行。修复：逐条检查——任何一条含拉丁字母却无目标文字体系字符即判错语种重试。
- **中文残留被剔成病句**（RU"С前面 есть молнияный карман"被剔成"С  есть молниянный карман"）：`_hygiene` 直接剔除中文字符，留下语法残缺的译文。修复：残留先单条重译（最多 3 次，校验无中文且语种正确），重译失败才剔除。
- **标签模式长译文一词一行溢出标签**（折叠行李包 RU 绿色 hashtag）：字号下限 floor=框高30%，俄语长单词在下限时只能一词一行、3 行总高溢出标签底部。修复：label_mode 字号下探放宽到 9px，且候选按"行数最少、同字号最大"选取，优先更少行数完整落在标签内。
- **GLM 把个别条目译成英语**（PT 版"Folding up"）：提示词约束不足。修复：系统提示加"每一条都必须全部使用目标语言，严禁把任何一条译成英语"。
- **粗体大标题纯白底留灰色鬼影**（双肩包"大容量多隔层"）：平涂定色条件要求框内均匀度 uni≥0.65，粗体大标题文字占比高把 uni 拉低，纯白底反而漏掉平涂，LaMa 填充色差几级成灰影。修复：平涂条件改为"底色亮度>235 且纹理梯度<60"——纹理守卫已能排除网格/织物，uni 要求反而误伤。
- **纹理守卫把粗笔画梯度误算成背景纹理**：第一版守卫对全框统计 Sobel 梯度，粗黑大字边缘梯度极高，纯白底被判成"纹理背景"跳过平涂，灰鬼影依旧。修复：只在接近底色（±12 级）的像素上统计梯度；二次定色核膨胀从 25 提到 45，盖住羽化尾区（粗笔画抗锯齿边缘低透明度混回会形成 229 级灰影）。

## 2026-09-01 干湿分离字号过小（v1.7续七）

- **白字深底框译文缩到 9px 看不清**（干湿分离）：两层原因叠加——①"单行优先"规则对 ≤3 词译文只要求单行字号≥多行 30%，单行 9px 即可挤掉两行 17px；②单行兜底保险强制宽度不超原框，水平外扩换来的宽度被压回，字号随宽度缩到 9。修复：新增 dark_roomy 模式（白字 fl>170 压深色照片底 bl<140 且非标签/边框框）：水平外扩 1.9、单行优先阈值提到 75%、单行宽度保险豁免（仍受邻框左右限制）。

---

## 折叠行李包（790×1112，2026-09-02 第三轮反馈）

**现象1：标题右侧箭头图形被抹缺一块**
- 根因1：箭头贴标题框边缘，`_stroke_mask` 把从框外延伸进来的贴边图形连通块当成笔画一起抹掉
- 根因2：软过渡带 σ5 模糊把锐利箭头边缘糊掉
- 修复：① `_stroke_mask` 排除贴框边（且非图像边缘裁切）的连通块；② 平涂区与紧致抹除区外扩45px内禁用软带

**现象2：两处斑马线条带（白条纹与碎石地面）界限模糊、界限处发灰**
- 根因1：近白底+混合内容框（fl<110、bl>235、uni<0.5）走整块 LaMa 擦除，把白条纹和碎石糊成一片 → 新增紧致笔画路径（binary 收紧到 gray≤fl+70|彩色），只抹笔画
- 根因2：框级底色估计被框内碎石拉偏（bl=177），无法判定平涂 → 改用笔画外25px环带取样（亮度>200且纹理<60）判定平涂填色
- 根因3（复发）：原字形软光晕（灰180-220）在黑芯抹掉后残留成灰云 → 供体纹理重建 + 对比度增益（同干湿分离）

**现象3（连带发现）：GO TO TRAVEL 尾部被白色平涂盖住**
- 根因：`_apply_protect` 施加太晚，`_sm` 已被存进 `_flat_fills` → 改为 `_stroke_mask` 返回后立即 `_apply_protect(_sm)`；平涂区限定为"接近底色且与笔画连通"

**回归验证**：28 张（7图×4语）全部重跑，箭头完整锐利、两条斑马带界限清晰、保护区完好、其余图无回归。
